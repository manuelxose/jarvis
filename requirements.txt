# Requires Python 3.11. The cloned-voice worker has its own environment:
# see requirements-tts.txt and scripts/setup_tts_worker.bat.

# Speech
faster-whisper==1.0.3
openwakeword==0.6.0
# Optional XTTS voice (tts.provider: local). coqui-tts pins transformers 4.x and torch.
coqui-tts[cpu]==0.27.5
transformers==4.57.1
torch==2.8.0
torchaudio==2.8.0

# Whisper on NVIDIA GPUs (stt.device: cuda): cuBLAS 12 + cuDNN 9 DLLs for CTranslate2.
# Without an NVIDIA GPU, WhisperSTT falls back to CPU automatically.
nvidia-cublas-cu12==12.4.5.8; sys_platform == "win32"
nvidia-cudnn-cu12==9.1.0.70; sys_platform == "win32"

# Audio
sounddevice==0.4.7
soundfile==0.12.1
numpy==1.26.4

# Optional cloud voice (Alibaba Model Studio)
dashscope==1.27.6

# Windows desktop integration (SAPI voice, windows, shortcuts, processes)
comtypes==1.4.12
pywin32==312; sys_platform == "win32"
psutil==5.9.8
